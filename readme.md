1、online 这个文件夹存放的是最初的算法
    online.py 就是目前线上运行的算法
    claude_kexing.py 开发中使用的

2、CuttingStockOptimizer 这个文件夹存放的是用gemini最新生成的算法，算法比较简洁。测试过几组数据和线上的计算结果一致
    如果线上版本有问题可以使用 该 文件夹中的 gemini_kexing_json.py 来代替线上的版本，上线后来测试效果

3、SteelPipeCuttingOptimizer 文件夹中，是要做新的算法来计算新的需求，需求如下

已知：

母材长度：y 米，即 y×1000 毫米。

每一根母材需要切掉头部 a 毫米、尾部 b 毫米（这两部分废弃）。
每切一次（切断）会有 c 毫米的锯缝损失（切损）。

要切的零件规格是：

(y1 毫米,x1 根),(y2 毫米,x2 根),…,(yn 毫米,xn 根)

这一组 (y1,x1),…,(yn,xn) 叫做一套（即生产一套需要每种零件分别做对应的数量）。

现有母材数量为 x 根（每根长度一样）。

问：这些母材一共可以生产多少套这样的零件？

我最需要的结果是最优解，计算速度可以放慢，但是必须是最优解，所以请选择合适的算法。使用python给出完整代码
需要的结果：和之前一样，打印关键信息，给出json


--------------------------------------------------------------------------------------

注意：gemini 生成的算法都有过度生成的问题，数量差距太大，无法使用

注意：线上版本也有过度生产的问题。

过度生成可能是这种算法的问题。

--------------------------------------------------------------------------------------